{% sw_extends '@Storefront/storefront/component/checkout/offcanvas-item.html.twig' %}

{% block component_offcanvas_product_image_inner %}
    {% if lineItem.type != 'tecsafe-ofcp' %}
        {{ parent() }}
    {% else %}
        <button onclick="window.postMessage('tecsafe:ofcp:open:cart', window.origin)">Open OFCP Cart</button>
    {% endif %}
{% endblock %}
